    name: Scheduled Workflow
    on:
      schedule:
        - cron: '*/2 * * * *'
    jobs:
      run_scheduled_task:
        runs-on: ubuntu-latest
        steps:
          - name: Run a script
            run: |
              echo "This job runs on a schedule!"
